#include "main.h"  // Include the header file with the function declaration

int main(void)
{
    int n = 'a';  // Replace 'c' with the character you want to check
    int r;


r = _isalpha(n);

    return 0;
}
